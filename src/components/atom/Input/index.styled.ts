import styled from 'styled-components';

export const Input = styled.input`
  display: flex;
  width: 100%;
  padding: 0 2rem;
  font-size: 1.6rem;
  background-color: transparent;

  &::placeholder {
    color: #ccc;
  }
`;
